# This was originally to ensure pulling in format and print from C++23,
# while testing C++20 coroutines. Now this is to ensure reproducible build.
FROM ubuntu:noble

RUN apt update && apt install -yq build-essential
